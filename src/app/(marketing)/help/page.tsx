import React from "react";
import { HelpContent } from "@/components/marketing/help-content";

export const metadata = {
  title: "Help Center | Vortex",
  description:
    "Find answers to common questions about getting started, your account, library, reviews, privacy, and billing on Vortex.",
};

export default function HelpPage() {
  return <HelpContent />;
}
