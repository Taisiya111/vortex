import React from "react";
import { AboutContent } from "@/components/marketing/about-content";

export const metadata = {
  title: "About | Vortex",
  description:
    "Learn about Vortex's mission to build the home PC gaming deserves — a place to track, rate, and discover games with a community that gets it.",
};

export default function AboutPage() {
  return <AboutContent />;
}
